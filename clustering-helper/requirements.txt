# Runtime deps for haritica-clustering-helper (GPL-3 aggregate).
# Must match helper.py's actual imports — v1.0.2 dropped scanpy/numba/llvmlite
# in favor of sklearn.neighbors + leidenalg/louvain directly (see helper.py
# and pyinstaller.spec excludes). scikit-learn and h5py are listed explicitly
# (not just pulled transitively) so a clean CI runner builds deterministically:
# helper.py imports sklearn.neighbors, and anndata.read_h5ad needs h5py.
#
# Versions are PINNED to the newest releases that still publish manylinux2014
# (glibc 2.17) cp311 wheels. The Linux desktop binary is built in a
# manylinux2014 container for maximum compatibility (runs on Ubuntu 20.04 /
# Debian 10 / RHEL 8 and newer); the latest scipy/scikit-learn dropped
# glibc-2.17 wheels (they now require manylinux_2_28), which forced a source
# build that failed for lack of OpenBLAS. These pins also ship macOS
# arm64/x86_64 + Windows cp311 wheels, so all four platforms build from wheels
# off the same versions (reproducible clustering).
anndata==0.10.9
h5py==3.11.0
scikit-learn==1.5.2
scipy==1.13.1
numpy==1.26.4
pandas==2.2.3
leidenalg==0.10.2
louvain==0.8.2
igraph==0.11.6
# louvain/__init__.py imports `pkg_resources` at load time. pkg_resources was
# REMOVED in setuptools 81.0 (2025-07), and the minimal manylinux2014 cp311
# Python ships no setuptools at all — so the build (and the bundled binary's
# `import louvain`) needs an older setuptools that still provides pkg_resources.
# GitHub's setup-python already bundles a <81 setuptools (why macOS/Windows
# build fine); this makes the Linux/manylinux build match.
setuptools<81
